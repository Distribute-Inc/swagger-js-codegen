const req = {
  method: '{{method}}',
  url: domain + path,
  qs: queryParameters,
  headers: headers,
  data: body
};
if( Object.keys(form).length > 0 ) {
  req.form = form;
}
if( typeof(body) === 'object' && !(body instanceof Buffer) ) {
  req.json = true;
}

const promise = axios(req).then(
  (successResponse) => {
    return successResponse;
  },
  (error) => {
    error.response;
  }
);
